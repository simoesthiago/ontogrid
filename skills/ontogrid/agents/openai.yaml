interface:
  display_name: "OntoGrid"
  short_description: "OntoGrid Energy Data Hub domain and repo guide"
  default_prompt: "Use $ontogrid to work within the OntoGrid Energy Data Hub MVP contracts and repository rules."

policy:
  allow_implicit_invocation: true
